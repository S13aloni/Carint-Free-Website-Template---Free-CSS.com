let newArray=false
      newArray = pase_data.filter(function (check_email) 
      {
        var string_email=JSON.stringify(check_email.Email_ls)
        var value_email=string_email.replace(/"/g, '')
        alert("newvalue: "+value_email)
          if(value_email==email){
            return true;
          }
          else{
            validation_email()
          }
        });
      if(newArray==true){
        alert("Good")
      }
      else{
        alert("Bad")
      }